// activity_main2.xml layout text - DELETE FROM CODE

<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="fill_parent"
    android:layout_height="fill_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="asb.deafcinemaconnect.Main2Activity"
    android:background="#21b6a8">

    <!-- TODO: Remove header image, SMS page -->


    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="phone"
        android:ems="12"
        android:id="@+id/numberEditText"
        android:hint="@string/numberEditText"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_alignEnd="@+id/textFriendsButton"
        android:textColor="#FFD197" />

    <Space
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_below="@+id/numberEditText"
        android:layout_alignParentStart="true"
        android:id="@+id/space" />

    <EditText
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:id="@+id/SMSEditText"
        android:background="#CBFFFA"
        android:hint="@string/SMSEditText"
        android:maxLength="160"
        android:inputType="textMultiLine|textAutoComplete|textAutoCorrect"
        android:nestedScrollingEnabled="true"
        android:layout_above="@+id/returnMainButton"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="56dp"
        android:layout_toStartOf="@+id/textFriendsButton" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/textFriendsButton"
        android:id="@+id/textFriendsButton"
        android:elegantTextHeight="false"
        android:layout_alignTop="@+id/SMSEditText"
        android:layout_alignParentEnd="true"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:background="#7f1917"
        android:textColor="#FFD197" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/returnMainButton"
        android:id="@+id/returnMainButton"
        android:layout_alignParentBottom="true"
        android:layout_toStartOf="@+id/textFriendsButton"
        android:background="#7f1917"
        android:textColor="#FFD197" />

</RelativeLayout>




// Main2Activity.java - DELETE FROM CODE

package asb.deafcinemaconnect;

import android.content.Intent;
import android.os.Bundle;
import android.app.Activity;
import android.telephony.SmsManager;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;
import android.widget.Toast;

public class Main2Activity extends Activity {

    EditText phoneNumber;
    EditText textMessage;
    Button sendSMSButton;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main2);

        phoneNumber = (EditText) findViewById(R.id.numberEditText);
        textMessage = (EditText) findViewById(R.id.SMSEditText);
        sendSMSButton = (Button) findViewById(R.id.textFriendsButton);

        sendSMSButton.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick(View arg0) {

                sendSms();

            }
        });

        Button returnMainButton = (Button)findViewById(R.id.returnMainButton);

        returnMainButton.setOnClickListener(new View.OnClickListener() {

            @Override
            public void onClick(View view) {
                Intent intent = new Intent(Main2Activity.this, MainActivity.class);
                startActivity(intent);
            }

        });

    } // end onCreate

        protected void sendSms(){

        String number = phoneNumber.getText().toString();
        String message = textMessage.getText().toString();

        SmsManager manager = SmsManager.getDefault();
        manager.sendTextMessage(number, null, message, null, null);
        Toast.makeText(getApplicationContext(), "Sent successfully!", Toast.LENGTH_LONG).show();
    }


    } // end sendSMS
